# Home Assistant 重新迁移到树莓派验证

- 日期：2026-05-18
- 源配置目录：`/Users/pataphaw/homeassistant/config`
- 目标主机：`raspberry@192.168.0.104`
- 目标部署目录：`/home/raspberry/homeassistant`
- 访问地址：`http://192.168.0.104:8123`

## 执行记录

- 确认源容器 `homeassistant` 已停止，源配置目录约 `66M`。
- 确认目标系统为 Debian GNU/Linux 13 trixie，架构 `aarch64`，根分区迁移前可用约 `7.6G`。
- 在目标主机安装 `docker.io` 与 `docker-compose`：
  - Docker version `26.1.5+dfsg1`
  - Docker Compose version `2.26.1-4`
- 将 `raspberry` 加入 `docker` 组，并启用 Docker 服务。
- 创建远端目录 `/home/raspberry/homeassistant/config`。
- 写入 `/home/raspberry/homeassistant/docker-compose.yml`，使用：
  - 镜像 `ghcr.io/home-assistant/home-assistant:stable`
  - `network_mode: host`
  - `restart: unless-stopped`
  - `/home/raspberry/homeassistant/config:/config`
- 使用 `rsync -a --delete` 同步本机配置到远端，排除：
  - `.ha_run.lock`
  - `home-assistant.log*`
- 备份远端配置为 `configuration.yaml.before-2026-05-18-pi-url`。
- 将远端 `configuration.yaml` 的 `external_url` 和 `internal_url` 改为 `http://192.168.0.104:8123`。
- 拉取 Home Assistant 镜像并启动容器。

## 验证结果

- 容器状态正常：
  - 名称：`homeassistant`
  - 镜像：`ghcr.io/home-assistant/home-assistant:stable`
  - 状态：`Up`
- Home Assistant 进程监听 `0.0.0.0:8123` 和 `:::8123`。
- `pataphaw-jr` 使用当前 `config.yaml` 成功查询关键实体：
  - entity_id：`climate.lumi_mcn02_d56f_air_conditioner`
  - state：`off`
  - friendly_name：`主卧空调 空调`
  - temperature：`19.0`
  - fan_mode：`Low`
- 本机 `config.yaml` 已指向 `http://192.168.0.104:8123`，并保持 `trust_env: true`。

## 观察到的问题

- Home Assistant 首次启动早期 API 返回过 `503 Service Unavailable`，等待初始化完成后恢复正常。
- 目标主机同时有两个局域网地址：
  - `wlan0`: `192.168.0.104`
  - `eth0`: `192.168.0.109`
- 目标主机系统时间查询结果为 `2026-05-19 06:18:10 CST`；本次操作跨 2026-05-18 / 2026-05-19，需要留意 Docker 状态中的相对时间显示。
- 启动期间 `192.168.0.104` 的 SSH/ping 出现过短暂超时，但后续 8123 与 API 验证可用；`192.168.0.109` 在排查期间也可访问 8123。
- 日志中仍出现与上次迁移类似的 `xiaomi_miot` 设备发现失败：
  - 空气净化器 `192.168.0.112`
  - Mi Smart LED Ceiling Light `192.168.0.107`
- 这些设备发现失败没有阻塞 Home Assistant 启动，也没有阻塞主卧空调实体查询。

## 结论

Home Assistant 已重新迁移并运行在树莓派 Docker Container 中。关键实体 API 查询通过，`pataphaw-jr` 的 Home Assistant 连接验证通过。
